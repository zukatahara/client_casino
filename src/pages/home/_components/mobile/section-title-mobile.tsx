const SectionTitleMobile = ({ title }: { title: string }) => {
  return (
    <div className="section-title2 ">
      <span className="name2">{title}</span>
    </div>
  );
};

export default SectionTitleMobile;
